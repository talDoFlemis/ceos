---
import { Icon } from "astro-icon";
const { title, data, url } = Astro.props;
---

<div class="space-y-2 w-full max-w-6xl">
  <div class="relative px-6 pt-8 pb-4 border bg-offset border-default">
    <h3
      class="absolute top-0 right-4 py-1 px-4 text-xs tracking-tight uppercase rounded-full border border-current -translate-y-1/2 bg-default"
    >
      {title}
    </h3>
    <ul class="grid grid-cols-2 gap-8 sm:grid-cols-3 md:grid-cols-6">
      {
        data.map(({ title, icon, url }) => (
          <li>
            <a class="flex flex-col items-center gap-2" href={url}>
              <Icon class="h-12" name={icon} />
              <span>{title}</span>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
  <p class="text-sm text-right">
    <a class="text-primary" href={url}> ...e muito mais &rarr;</a>
  </p>
</div>
